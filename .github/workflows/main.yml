name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Set up python env
        run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
      - name: Run a test
        run: python razplusfreetrial.py
